[project]
name = "comfyui-minimax-h3-parallel"
description = "Exact activation-only multi-GPU attention head sharding for MiniMax H3 in ComfyUI."
version = "0.1.0"
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.urls]
Repository = "https://github.com/AesSedai/ComfyUI-MiniMaxH3-Parallel"
